package com.ftn.sbnz.repo;

import com.ftn.sbnz.model.CriticalPeriod;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CriticalPeriodRepository extends JpaRepository<CriticalPeriod, Long> {
}